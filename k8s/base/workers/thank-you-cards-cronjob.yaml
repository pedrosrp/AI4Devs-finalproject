apiVersion: batch/v1
kind: CronJob
metadata:
  name: thank-you-cards-worker
  namespace: aura
spec:
  schedule: "0 10 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: thank-you-cards-worker
        spec:
          imagePullSecrets:
          - name: ghcr-secret
          containers:
          - name: worker
            image: aura-workers-thankyoucards:latest
            envFrom:
            - configMapRef:
                name: aura-app-configmap
            - secretRef:
                name: aura-app-secret
            - secretRef:
                name: api-secrets
          restartPolicy: OnFailure
